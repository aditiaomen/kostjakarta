<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>KostSpace | Dasbor Admin & Pemilik Kost</title>
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Google Fonts: Plus Jakarta Sans -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        sans: ['"Plus Jakarta Sans"', 'sans-serif'],
                    },
                    colors: {
                        brand: {
                            50: '#eff6ff',
                            100: '#dbeafe',
                            500: '#3b82f6',
                            600: '#2563eb',
                            700: '#1d4ed8',
                            900: '#1e3a8a',
                        }
                    },
                    boxShadow: {
                        'soft': '0 4px 30px -2px rgba(0, 0, 0, 0.03)',
                        'float': '0 12px 40px -8px rgba(37, 99, 235, 0.12)',
                        'premium': '0 20px 50px -12px rgba(15, 23, 42, 0.08)'
                    }
                }
            }
        }
    </script>
    <style>
        body { font-family: 'Plus Jakarta Sans', sans-serif; overflow: hidden; background-color: #f8fafc; }
        .page { display: none; animation: fadeIn 0.4s cubic-bezier(0.16, 1, 0.3, 1) forwards; }
        .page.active { display: block; }
        @keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }
        
        ::-webkit-scrollbar { width: 6px; height: 6px; }
        ::-webkit-scrollbar-track { background: transparent; }
        ::-webkit-scrollbar-thumb { background: #cbd5e1; border-radius: 10px; }
        ::-webkit-scrollbar-thumb:hover { background: #94a3b8; }
        
        /* Spinner Loader */
        .loader { border: 3px solid #f1f5f9; border-radius: 50%; border-top: 3px solid #2563eb; width: 24px; height: 24px; animation: spin 0.8s linear infinite; }
        @keyframes spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
    </style>
</head>
<body class="text-slate-800 antialiased flex h-screen selection:bg-brand-500 selection:text-white">

    <!-- AUTHENTICATION SCREEN -->
    <div id="auth-screen" class="fixed inset-0 bg-slate-50 flex flex-col justify-center items-center z-[100] transition-all duration-300">
        <div class="w-full max-w-md p-8 sm:p-10 bg-white border border-slate-100 rounded-3xl shadow-premium">
            <div class="flex flex-col items-center gap-3 mb-8">
                <div class="w-12 h-12 bg-brand-600 rounded-2xl flex items-center justify-center shadow-lg shadow-brand-500/30">
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2.5" stroke="white" class="w-6 h-6">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M2.25 12l8.954-8.955c.44-.439 1.152-.439 1.591 0L21.75 12M4.5 9.75v10.125c0 .621.504 1.125 1.125 1.125H9.75v-4.875c0-.621.504-1.125 1.125-1.125h2.25c.621 0 1.125.504 1.125 1.125V21h4.125c.621 0 1.125-.504 1.125-1.125V9.75M8.25 21h8.25" />
                    </svg>
                </div>
                <h1 class="text-2xl font-extrabold tracking-tight text-slate-900">KostSpace Admin</h1>
                <p class="text-sm text-slate-500">Kelola properti & penyewa secara praktis</p>
            </div>
            <form id="login-form" class="space-y-5">
                <div>
                    <label class="block text-xs font-extrabold uppercase tracking-widest text-slate-400 mb-1.5">Email Akses</label>
                    <input type="email" value="admin@kostspace.com" required class="w-full px-4 py-2.5 bg-slate-50 border border-slate-200 rounded-xl focus:ring-2 focus:ring-brand-500 outline-none transition-all text-sm font-semibold">
                </div>
                <div>
                    <label class="block text-xs font-extrabold uppercase tracking-widest text-slate-400 mb-1.5">Kata Sandi</label>
                    <input type="password" value="password" required class="w-full px-4 py-2.5 bg-slate-50 border border-slate-200 rounded-xl focus:ring-2 focus:ring-brand-500 outline-none transition-all text-sm font-semibold">
                </div>
                <button type="submit" class="w-full bg-slate-900 text-white font-bold py-3 rounded-xl hover:bg-slate-800 active:scale-[0.98] transition-all flex justify-center items-center gap-2">
                    <span id="login-text">Masuk Sekarang</span>
                    <div id="login-loader" class="loader hidden w-5 h-5 border-2"></div>
                </button>
                <p class="text-xs text-center text-slate-400 font-medium">Klik langsung tombol masuk untuk demo testing</p>
            </form>
        </div>
    </div>

    <!-- MAIN DASHBOARD SHELL (Hidden initially) -->
    <div id="app-shell" class="w-full h-full flex hidden">
        
        <!-- Sidebar Navigation -->
        <aside id="sidebar" class="fixed inset-y-0 left-0 z-40 w-64 bg-white border-r border-slate-150 transform -translate-x-full lg:translate-x-0 lg:static transition-transform duration-300 flex flex-col shadow-premium lg:shadow-none">
            <!-- Brand Logo -->
            <div class="h-20 flex items-center px-6 border-b border-slate-100 shrink-0">
                <div class="flex items-center gap-2.5">
                    <div class="w-8 h-8 bg-brand-600 rounded-xl flex items-center justify-center shadow-md shadow-brand-500/20">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2.5" stroke="white" class="w-4 h-4">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M2.25 12l8.954-8.955c.44-.439 1.152-.439 1.591 0L21.75 12M4.5 9.75v10.125c0 .621.504 1.125 1.125 1.125H9.75v-4.875c0-.621.504-1.125 1.125-1.125h2.25c.621 0 1.125.504 1.125 1.125V21h4.125c.621 0 1.125-.504 1.125-1.125V9.75M8.25 21h8.25" />
                        </svg>
                    </div>
                    <span class="text-lg font-extrabold tracking-tight text-slate-950">KostSpace<span class="text-brand-600">.</span></span>
                </div>
            </div>

            <!-- Nav Links -->
            <nav class="flex-1 overflow-y-auto py-6 px-4 space-y-1.5" id="nav-links">
                <span class="block text-[10px] font-bold text-slate-400 uppercase tracking-widest px-3 mb-2">Menu Utama</span>
                <a href="#" data-route="dashboard" class="nav-item active flex items-center gap-3 px-3 py-2.5 bg-brand-50 text-brand-600 rounded-xl font-bold text-sm transition-all">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.2"><rect width="7" height="9" x="3" y="3" rx="1"/><rect width="7" height="5" x="14" y="3" rx="1"/><rect width="7" height="9" x="14" y="12" rx="1"/><rect width="7" height="5" x="3" y="16" rx="1"/></svg> Dashboard
                </a>
                <a href="#" data-route="properties" class="nav-item flex items-center gap-3 px-3 py-2.5 text-slate-500 hover:bg-slate-50 hover:text-slate-950 rounded-xl font-bold text-sm transition-all">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.2"><path stroke-linecap="round" stroke-linejoin="round" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"/></svg> Properti Kost
                </a>
                <a href="#" data-route="tenants" class="nav-item flex items-center gap-3 px-3 py-2.5 text-slate-500 hover:bg-slate-50 hover:text-slate-950 rounded-xl font-bold text-sm transition-all">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.2"><path stroke-linecap="round" stroke-linejoin="round" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"/></svg> Penghuni Kost
                </a>
                <a href="#" data-route="finances" class="nav-item flex items-center gap-3 px-3 py-2.5 text-slate-500 hover:bg-slate-50 hover:text-slate-950 rounded-xl font-bold text-sm transition-all">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.2"><rect width="20" height="14" x="2" y="5" rx="2"/><line x1="2" x2="22" y1="10" y2="10"/></svg> Keuangan & Tagihan
                </a>
                <a href="#" data-route="messages" class="nav-item flex items-center justify-between px-3 py-2.5 text-slate-500 hover:bg-slate-50 hover:text-slate-950 rounded-xl font-bold text-sm transition-all">
                    <div class="flex items-center gap-3">
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.2"><path stroke-linecap="round" stroke-linejoin="round" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"/></svg> Pesan Masuk
                    </div>
                    <span class="bg-brand-100 text-brand-700 py-0.5 px-2 rounded-full text-[10px] font-extrabold">2</span>
                </a>
            </nav>

            <!-- Bottom Section / Settings -->
            <div class="p-4 border-t border-slate-100 shrink-0">
                <a href="#" data-route="settings" class="nav-item flex items-center gap-3 px-3 py-2.5 text-slate-500 hover:bg-slate-50 hover:text-slate-950 rounded-xl font-bold text-sm transition-all mb-3">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.2"><path stroke-linecap="round" stroke-linejoin="round" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/></svg> Pengaturan
                </a>
                
                <div class="flex items-center gap-3 px-3 py-2 bg-slate-50 border border-slate-100 rounded-2xl">
                    <img src="https://ui-avatars.com/api/?name=Admin+KostSpace&background=2563eb&color=fff" alt="Profil" class="w-8 h-8 rounded-xl border border-slate-200">
                    <div class="flex-1 min-w-0">
                        <p class="text-xs font-bold text-slate-900 truncate">PIC Admin</p>
                        <p class="text-[10px] text-slate-500 font-semibold truncate">KostSpace Corp</p>
                    </div>
                    <button onclick="logout()" class="text-slate-400 hover:text-rose-600 transition-colors" title="Keluar">
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.5"><path stroke-linecap="round" stroke-linejoin="round" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/></svg>
                    </button>
                </div>
            </div>
        </aside>

        <!-- Main Workspace Area -->
        <div class="flex-1 flex flex-col min-w-0">
            <!-- Header Top Bar -->
            <header class="h-20 bg-white border-b border-slate-150 flex items-center justify-between px-6 shrink-0">
                <div class="flex items-center gap-4">
                    <button onclick="toggleSidebar()" class="lg:hidden text-slate-500 hover:text-slate-900 focus:outline-none p-2 hover:bg-slate-50 rounded-xl">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.2"><line x1="4" x2="20" y1="12" y2="12"/><line x1="4" x2="20" y1="6" y2="6"/><line x1="4" x2="20" y1="18" y2="18"/></svg>
                    </button>
                    
                    <nav class="hidden sm:flex items-center text-sm font-semibold text-slate-500">
                        <a href="#" class="hover:text-slate-900">Dasbor Admin</a>
                        <svg class="w-4 h-4 mx-2" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.5"><path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/></svg>
                        <span class="text-slate-900" id="breadcrumbs-title">Ringkasan Analitik</span>
                    </nav>
                </div>

                <div class="flex items-center gap-4">
                    <div class="relative hidden md:block">
                        <svg class="w-4 h-4 absolute left-3.5 top-1/2 -translate-y-1/2 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.2"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.3-4.3"/></svg>
                        <input type="text" placeholder="Cari data, nama kost..." class="w-64 pl-10 pr-4 py-2 bg-slate-50 border border-slate-200 rounded-full text-xs font-bold outline-none focus:ring-2 focus:ring-brand-500 transition-all">
                    </div>
                </div>
            </header>

            <!-- Dynamic Dashboard Pages Container -->
            <main class="flex-1 overflow-y-auto p-6 sm:p-8">
                
                <!-- PAGE: DASHBOARD (OVERVIEW) -->
                <section id="page-dashboard" class="page active space-y-6 max-w-6xl mx-auto">
                    <div>
                        <h1 class="text-2xl font-extrabold text-slate-950 tracking-tight">Selamat Datang, Admin!</h1>
                        <p class="text-slate-500 text-sm mt-0.5">Analisis performa listing KostSpace di Jakarta secara waktu nyata (*real-time*).</p>
                    </div>

                    <!-- Stats Card Grid -->
                    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4" id="stats-container">
                        <!-- Populated Dynamic Stats via JS -->
                    </div>

                    <!-- Main Graph Mockup & System Feed -->
                    <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
                        <div class="lg:col-span-2 bg-white p-6 border border-slate-150 rounded-3xl shadow-soft">
                            <div class="flex justify-between items-center mb-6">
                                <h3 class="font-extrabold text-slate-900 text-sm">Grafik Estimasi Pendapatan</h3>
                                <span class="bg-brand-50 text-brand-600 text-[10px] font-extrabold px-3 py-1.5 rounded-lg border border-brand-100 uppercase tracking-wider">Per Bulan</span>
                            </div>
                            <div class="h-44 flex items-end gap-3 justify-between">
                                <div class="w-full bg-slate-100 rounded-t-xl h-[40%] relative group cursor-pointer hover:bg-brand-500 transition-all"><div class="absolute -top-8 left-1/2 -translate-x-1/2 bg-slate-900 text-white text-[9px] px-2 py-1 rounded-md opacity-0 group-hover:opacity-100 transition-opacity">IDR 3.5jt</div></div>
                                <div class="w-full bg-slate-100 rounded-t-xl h-[55%] relative group cursor-pointer hover:bg-brand-500 transition-all"><div class="absolute -top-8 left-1/2 -translate-x-1/2 bg-slate-900 text-white text-[9px] px-2 py-1 rounded-md opacity-0 group-hover:opacity-100 transition-opacity">IDR 5.6jt</div></div>
                                <div class="w-full bg-slate-100 rounded-t-xl h-[35%] relative group cursor-pointer hover:bg-brand-500 transition-all"><div class="absolute -top-8 left-1/2 -translate-x-1/2 bg-slate-900 text-white text-[9px] px-2 py-1 rounded-md opacity-0 group-hover:opacity-100 transition-opacity">IDR 2.1jt</div></div>
                                <div class="w-full bg-brand-600 rounded-t-xl h-[85%] relative group cursor-pointer shadow-float"><div class="absolute -top-8 left-1/2 -translate-x-1/2 bg-slate-900 text-white text-[9px] px-2 py-1 rounded-md opacity-0 group-hover:opacity-100 transition-opacity">IDR 10jt</div></div>
                                <div class="w-full bg-slate-100 rounded-t-xl h-[65%] relative group cursor-pointer hover:bg-brand-500 transition-all"><div class="absolute -top-8 left-1/2 -translate-x-1/2 bg-slate-900 text-white text-[9px] px-2 py-1 rounded-md opacity-0 group-hover:opacity-100 transition-opacity">IDR 6.5jt</div></div>
                                <div class="w-full bg-slate-100 rounded-t-xl h-[45%] relative group cursor-pointer hover:bg-brand-500 transition-all"><div class="absolute -top-8 left-1/2 -translate-x-1/2 bg-slate-900 text-white text-[9px] px-2 py-1 rounded-md opacity-0 group-hover:opacity-100 transition-opacity">IDR 4.2jt</div></div>
                            </div>
                            <div class="flex justify-between text-[10px] font-extrabold text-slate-400 mt-4 uppercase tracking-wider">
                                <span>Jan</span><span>Feb</span><span>Mar</span><span>Apr</span><span>Mei</span><span>Jun</span>
                            </div>
                        </div>

                        <div class="bg-white p-6 border border-slate-150 rounded-3xl shadow-soft flex flex-col justify-between">
                            <div>
                                <h3 class="font-extrabold text-slate-900 text-sm mb-4">Aktivitas Sistem</h3>
                                <div class="space-y-4" id="activity-feed">
                                    <div class="flex gap-3 text-xs">
                                        <div class="w-2.5 h-2.5 rounded-full bg-emerald-500 shrink-0 mt-1"></div>
                                        <div><p class="font-bold text-slate-800">Koneksi Database Berhasil</p><p class="text-[10px] text-slate-400 font-semibold">Baru Saja</p></div>
                                    </div>
                                </div>
                            </div>
                            <button onclick="showToast('Sistem di-refresh secara realtime')" class="w-full bg-slate-100 hover:bg-slate-200 text-slate-800 font-bold text-xs py-3 rounded-2xl transition-colors mt-4">Refresh Feed</button>
                        </div>
                    </div>
                </section>

                <!-- PAGE: PROPERTIES -->
                <section id="page-properties" class="page space-y-6 max-w-6xl mx-auto">
                    <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-4">
                        <div>
                            <h1 class="text-2xl font-extrabold text-slate-950 tracking-tight">Manajemen Properti Kost</h1>
                            <p class="text-slate-500 text-sm mt-0.5">Kelola, tambah, perbarui, dan hapus listing kost aktif Anda.</p>
                        </div>
                        <button onclick="openPropertyModal()" class="px-5 py-3 bg-slate-900 hover:bg-slate-800 text-white text-xs font-bold rounded-2xl shadow-sm transition-all flex items-center gap-2 active:scale-95 shrink-0">
                            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.5"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
                            Tambah Properti
                        </button>
                    </div>

                    <!-- Listing Data Card Table -->
                    <div class="bg-white border border-slate-150 rounded-3xl shadow-soft overflow-hidden flex flex-col">
                        <div class="p-4 sm:p-5 border-b border-slate-100 flex flex-col sm:flex-row sm:items-center justify-between gap-4 bg-slate-50/50">
                            <div class="relative w-full max-w-sm">
                                <svg class="w-4 h-4 absolute left-3 top-1/2 -translate-y-1/2 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.2"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.3-4.3"/></svg>
                                <input type="text" id="searchInput" placeholder="Cari nama kost atau wilayah..." class="w-full pl-9 pr-4 py-2 border border-slate-200 rounded-xl text-xs font-bold outline-none focus:ring-2 focus:ring-brand-500 transition-all">
                            </div>
                        </div>

                        <div class="overflow-x-auto min-h-[300px] relative">
                            <!-- Loader -->
                            <div id="table-loader" class="absolute inset-0 bg-white/85 flex flex-col items-center justify-center z-10 hidden">
                                <div class="loader mb-2"></div>
                                <span class="text-xs font-bold text-slate-400">Sinkronisasi Server...</span>
                            </div>

                            <table class="w-full text-left text-xs whitespace-nowrap">
                                <thead class="bg-slate-50/80 border-b border-slate-100 text-slate-400 font-extrabold uppercase tracking-widest text-[10px]">
                                    <tr>
                                        <th class="px-6 py-4">Nama Properti</th>
                                        <th class="px-6 py-4">Wilayah & Tipe</th>
                                        <th class="px-6 py-4">Sewa Bulanan</th>
                                        <th class="px-6 py-4">Ketersediaan</th>
                                        <th class="px-6 py-4 text-right">Aksi</th>
                                    </tr>
                                </thead>
                                <tbody id="properties-tbody" class="divide-y divide-slate-100 bg-white">
                                    <!-- Populated dynamically -->
                                </tbody>
                            </table>

                            <!-- Empty State -->
                            <div id="empty-state" class="hidden p-12 text-center text-slate-500">
                                <p class="text-sm font-bold">Tidak ada properti kost ditemukan.</p>
                            </div>
                        </div>
                    </div>
                </section>

                <!-- PAGE: TENANTS (Indonesian Translated) -->
                <section id="page-tenants" class="page space-y-6 max-w-6xl mx-auto">
                    <h1 class="text-2xl font-extrabold text-slate-950 tracking-tight">Manajemen Penghuni Kost</h1>
                    <div class="bg-white p-10 border border-slate-150 rounded-3xl text-center shadow-soft">
                        <div class="w-14 h-14 bg-brand-50 text-brand-600 rounded-full flex items-center justify-center mx-auto mb-4">
                            <svg class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"/></svg>
                        </div>
                        <h3 class="text-lg font-bold text-slate-900">Portal Manajemen Penyewa</h3>
                        <p class="text-xs text-slate-500 max-w-md mx-auto mt-2 leading-relaxed font-semibold">Gunakan menu ini untuk memetakan penyewa aktif ke nomor kamar, memantau kontrak, hingga mengatur tanggal keluar-masuk kost.</p>
                    </div>
                </section>

                <!-- PAGE: FINANCES (Indonesian Translated) -->
                <section id="page-finances" class="page space-y-6 max-w-6xl mx-auto">
                    <h1 class="text-2xl font-extrabold text-slate-950 tracking-tight">Keuangan & Pencairan Dana</h1>
                    <div class="bg-white p-10 border border-slate-150 rounded-3xl text-center shadow-soft">
                        <div class="w-14 h-14 bg-brand-50 text-brand-600 rounded-full flex items-center justify-center mx-auto mb-4">
                            <svg class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2"><rect width="20" height="14" x="2" y="5" rx="2"/><line x1="2" x2="22" y1="10" y2="10"/></svg>
                        </div>
                        <h3 class="text-lg font-bold text-slate-900">Laporan Akuntansi & Mutasi</h3>
                        <p class="text-xs text-slate-500 max-w-md mx-auto mt-2 leading-relaxed font-semibold">Unduh rekapan tagihan penyewa bulanan, pantau status pembayaran transfer digital, serta atur nomor rekening bank PIC untuk pembayaran otomatis.</p>
                    </div>
                </section>

                <!-- PAGE: MESSAGES (Indonesian Translated) -->
                <section id="page-messages" class="page space-y-6 max-w-6xl mx-auto h-[calc(100vh-140px)] flex flex-col">
                    <h1 class="text-2xl font-extrabold text-slate-950 tracking-tight shrink-0">Pesan Masuk</h1>
                    <div class="flex-1 bg-white border border-slate-150 rounded-3xl shadow-soft overflow-hidden flex">
                        <div class="w-1/3 border-r border-slate-100 overflow-y-auto bg-slate-50/50">
                            <div class="p-4 border-b border-slate-100 bg-white cursor-pointer hover:bg-slate-50 relative">
                                <div class="font-bold text-sm text-slate-900">Budi Santoso</div>
                                <p class="text-slate-400 text-[11px] truncate mt-0.5 font-medium">Apakah kamar Kost Senopati masih ada AC?</p>
                                <span class="absolute top-4 right-4 w-2 h-2 bg-brand-500 rounded-full animate-pulse"></span>
                            </div>
                        </div>
                        <div class="flex-1 flex flex-col justify-center items-center text-slate-400 bg-white p-6">
                            <p class="text-xs font-bold text-slate-400">Pilih salah satu ruang percakapan untuk membalas pesan penyewa secara interaktif.</p>
                        </div>
                    </div>
                </section>

                <!-- PAGE: SETTINGS (Indonesian Translated) -->
                <section id="page-settings" class="page space-y-6 max-w-2xl mx-auto">
                    <h1 class="text-2xl font-extrabold text-slate-950 tracking-tight">Pengaturan Profil</h1>
                    <div class="bg-white p-6 sm:p-8 border border-slate-150 rounded-3xl shadow-soft">
                        <h3 class="font-extrabold text-slate-900 text-sm border-b border-slate-100 pb-3 mb-6">Informasi Dasar Admin</h3>
                        <div class="space-y-4 text-xs">
                            <div>
                                <label class="block text-[10px] font-extrabold uppercase tracking-widest text-slate-400 mb-1.5">Nama PIC Pengelola</label>
                                <input type="text" value="PIC Admin KostSpace" class="w-full px-4 py-2.5 bg-slate-50 border border-slate-200 rounded-xl outline-none font-semibold">
                            </div>
                            <div>
                                <label class="block text-[10px] font-extrabold uppercase tracking-widest text-slate-400 mb-1.5">Email PIC</label>
                                <input type="email" value="admin@kostspace.com" class="w-full px-4 py-2.5 bg-slate-50 border border-slate-200 rounded-xl outline-none font-semibold" disabled>
                            </div>
                            <button onclick="showToast('Profil berhasil disimpan.')" class="w-full bg-slate-900 text-white font-bold py-3 rounded-xl hover:bg-slate-800 transition-colors">Simpan Perubahan</button>
                        </div>
                    </div>
                </section>

            </main>
        </div>
    </div>

    <!-- ================= MODALS & POP-UPS ================= -->
    <div id="mobileOverlay" class="fixed inset-0 bg-slate-900/50 backdrop-blur-sm z-30 hidden lg:hidden" onclick="toggleSidebar()"></div>

    <!-- MODAL: ADD / EDIT PROPERTY -->
    <div id="propertyModal" class="fixed inset-0 bg-slate-900/60 backdrop-blur-sm z-50 hidden flex items-center justify-center p-4 opacity-0 transition-opacity duration-300">
        <div class="bg-white rounded-[2rem] shadow-premium w-full max-w-3xl max-h-[90vh] flex flex-col transform scale-95 transition-transform duration-300" id="propertyModalContent">
            
            <div class="p-6 border-b border-slate-100 flex justify-between items-center bg-slate-50/50 rounded-t-[2rem]">
                <h2 class="text-lg font-extrabold text-slate-900" id="modalTitle">Tambah Properti Baru</h2>
                <button onclick="closePropertyModal()" class="p-2 text-slate-400 hover:text-slate-900 bg-white border border-slate-100 rounded-full shadow-sm"><svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="18" y1="6" x2="6" y2="18"></line><line x1="6" y1="6" x2="18" y2="18"></line></svg></button>
            </div>

            <div class="p-6 overflow-y-auto flex-1 text-xs">
                <form id="propertyForm" class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <input type="hidden" id="propId">
                    
                    <div class="space-y-4 md:col-span-2">
                        <label class="block text-[10px] font-extrabold uppercase tracking-widest text-slate-400">Media Utama (Foto Kost)</label>
                        <!-- Image Upload Area -->
                        <div class="border-2 border-dashed border-slate-200 rounded-2xl p-6 text-center hover:bg-slate-50 transition-all cursor-pointer relative bg-slate-50/50" id="imageUploadArea">
                            <input type="file" id="propImage" accept="image/*" class="absolute inset-0 w-full h-full opacity-0 cursor-pointer">
                            <svg class="w-8 h-8 mx-auto text-slate-400 mb-2" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="17 8 12 3 7 8"/><line x1="12" x2="12" y1="3" y2="15"/></svg>
                            <p class="font-bold text-slate-800">Klik atau Drag & Drop Foto di Sini</p>
                            <p class="text-[10px] text-slate-400 font-semibold mt-0.5">JPEG, PNG, JPG (Maks. 5MB)</p>
                            <div id="imagePreview" class="hidden mt-4 mx-auto max-w-xs rounded-xl overflow-hidden border shadow-sm">
                                <img src="" alt="Preview" class="w-full h-36 object-cover">
                            </div>
                        </div>
                    </div>

                    <div class="space-y-4">
                        <div>
                            <label class="block text-[10px] font-extrabold uppercase tracking-widest text-slate-400 mb-1.5">Nama Properti *</label>
                            <input type="text" id="propName" required class="w-full px-4 py-2.5 bg-slate-50 border border-slate-200 rounded-xl outline-none focus:ring-2 focus:ring-brand-500 font-semibold">
                        </div>
                        <div>
                            <label class="block text-[10px] font-extrabold uppercase tracking-widest text-slate-400 mb-1.5">Wilayah Jakarta *</label>
                            <select id="propLocation" required class="w-full px-4 py-2.5 bg-slate-50 border border-slate-200 rounded-xl outline-none focus:ring-2 focus:ring-brand-500 font-semibold">
                                <option value="Jakarta Selatan">Jakarta Selatan</option>
                                <option value="Jakarta Pusat">Jakarta Pusat</option>
                                <option value="Jakarta Barat">Jakarta Barat</option>
                                <option value="Jakarta Utara">Jakarta Utara</option>
                            </select>
                        </div>
                        <div>
                            <label class="block text-[10px] font-extrabold uppercase tracking-widest text-slate-400 mb-1.5">Harga Dasar Sewa / Bln (Rp) *</label>
                            <input type="number" id="propPrice" required class="w-full px-4 py-2.5 bg-slate-50 border border-slate-200 rounded-xl outline-none focus:ring-2 focus:ring-brand-500 font-semibold">
                        </div>
                    </div>

                    <div class="space-y-4">
                        <div>
                            <label class="block text-[10px] font-extrabold uppercase tracking-widest text-slate-400 mb-1.5">Tipe Kost *</label>
                            <select id="propType" required class="w-full px-4 py-2.5 bg-slate-50 border border-slate-200 rounded-xl outline-none focus:ring-2 focus:ring-brand-500 font-semibold">
                                <option value="Campur">Campur</option>
                                <option value="Putra">Putra</option>
                                <option value="Putri">Putri</option>
                            </select>
                        </div>
                        <div>
                            <label class="block text-[10px] font-extrabold uppercase tracking-widest text-slate-400 mb-1.5">Status Ketersediaan *</label>
                            <select id="propAvailability" required class="w-full px-4 py-2.5 bg-slate-50 border border-slate-200 rounded-xl outline-none focus:ring-2 focus:ring-brand-500 font-semibold">
                                <option value="Tersedia">Tersedia</option>
                                <option value="Penuh">Penuh</option>
                            </select>
                        </div>
                        <div>
                            <label class="block text-[10px] font-extrabold uppercase tracking-widest text-slate-400 mb-1.5">WhatsApp Admin</label>
                            <input type="text" id="propWa" placeholder="08..." class="w-full px-4 py-2.5 bg-slate-50 border border-slate-200 rounded-xl outline-none focus:ring-2 focus:ring-brand-500 font-semibold">
                        </div>
                    </div>

                    <div class="md:col-span-2 space-y-4">
                        <div>
                            <label class="block text-[10px] font-extrabold uppercase tracking-widest text-slate-400 mb-1.5">Fasilitas Kost (Pisahkan dengan koma)</label>
                            <input type="text" id="propFacilities" placeholder="WiFi Cepat, AC, Kamar Mandi Dalam, Parkir Mobil" class="w-full px-4 py-2.5 bg-slate-50 border border-slate-200 rounded-xl outline-none focus:ring-2 focus:ring-brand-500 font-semibold">
                        </div>
                        <div>
                            <label class="block text-[10px] font-extrabold uppercase tracking-widest text-slate-400 mb-1.5">Deskripsi Lengkap</label>
                            <textarea id="propAddress" rows="3" class="w-full px-4 py-2.5 bg-slate-50 border border-slate-200 rounded-xl outline-none focus:ring-2 focus:ring-brand-500 font-semibold" placeholder="Jelaskan kenyamanan dan kelebihan properti kost Anda..."></textarea>
                        </div>
                    </div>
                </form>
            </div>

            <div class="p-6 border-t border-slate-100 bg-slate-50/50 rounded-b-[2rem] flex justify-end gap-3 shrink-0">
                <button onclick="closePropertyModal()" class="px-5 py-2.5 font-bold text-slate-600 bg-white border border-slate-200 rounded-xl hover:bg-slate-100 transition-colors">Batal</button>
                <button onclick="submitPropertyForm()" class="px-6 py-2.5 font-bold bg-slate-900 text-white rounded-xl hover:bg-slate-800 transition-colors flex items-center gap-2 active:scale-95 shadow-md shadow-slate-900/10">
                    <span id="saveBtnText">Simpan Properti</span>
                    <div id="saveLoader" class="loader hidden w-4 h-4 border-2 border-t-white"></div>
                </button>
            </div>
        </div>
    </div>

    <!-- MODAL: DELETE CONFIRMATION -->
    <div id="deleteModal" class="fixed inset-0 bg-slate-900/60 backdrop-blur-sm z-50 hidden flex items-center justify-center p-4 opacity-0 transition-opacity duration-300">
        <div class="bg-white rounded-[2rem] shadow-premium w-full max-w-sm p-6 text-center transform scale-95 transition-transform duration-300" id="deleteModalContent">
            <div class="w-14 h-14 bg-rose-150 text-rose-600 rounded-full flex items-center justify-center mx-auto mb-4 bg-rose-50 border border-rose-100">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.5"><path stroke-linecap="round" stroke-linejoin="round" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"/></svg>
            </div>
            <h3 class="text-lg font-extrabold text-slate-900 mb-2">Hapus Properti?</h3>
            <p class="text-xs font-semibold text-slate-500 mb-6 leading-relaxed">Apakah Anda yakin ingin menghapus kost ini secara permanen dari KostSpace? Tindakan ini tidak dapat dibatalkan.</p>
            <div class="flex gap-3 justify-center text-xs">
                <button onclick="closeDeleteModal()" class="flex-1 py-3 font-bold text-slate-700 bg-slate-100 hover:bg-slate-200 rounded-xl transition-colors">Batal</button>
                <button onclick="confirmDelete()" class="flex-1 py-3 font-bold text-white bg-rose-600 hover:bg-rose-700 rounded-xl transition-colors flex justify-center items-center gap-2">
                    <span id="delBtnText">Ya, Hapus</span>
                    <div id="delLoader" class="loader hidden w-4 h-4 border-2 border-t-white"></div>
                </button>
            </div>
        </div>
    </div>

    <!-- TOAST NOTIFICATION -->
    <div id="toast" class="fixed bottom-6 left-1/2 -translate-x-1/2 z-[60] transform translate-y-24 opacity-0 transition-all duration-500 pointer-events-none">
        <div class="bg-slate-900 text-white px-6 py-3.5 rounded-2xl shadow-premium font-bold text-xs flex items-center gap-3">
            <span id="toast-icon">✓</span>
            <span id="toast-msg">Operasi Berhasil</span>
        </div>
    </div>

    <!-- MAIN CORE LOGIC -->
    <script>
        /**
         * SETUP BACKEND INTEGRATION
         * Jika Anda mengaktifkan Google Apps Script Web App, tempatkan URL-nya di bawah.
         */
        const APPS_SCRIPT_URL = ''; 
        
        let state = {
            properties: [],
            deleteId: null,
            base64Image: null
        };

        // --- AUTHENTICATION FLOW ---
        document.getElementById('login-form').addEventListener('submit', (e) => {
            e.preventDefault();
            const btn = document.getElementById('login-text');
            const loader = document.getElementById('login-loader');
            btn.classList.add('hidden'); loader.classList.remove('hidden');
            
            setTimeout(() => {
                document.getElementById('auth-screen').style.opacity = '0';
                setTimeout(() => {
                    document.getElementById('auth-screen').classList.add('hidden');
                    document.getElementById('app-shell').classList.remove('hidden');
                    loadData(); 
                }, 300);
            }, 800);
        });

        function logout() {
            document.getElementById('app-shell').classList.add('hidden');
            document.getElementById('auth-screen').classList.remove('hidden');
            document.getElementById('auth-screen').style.opacity = '1';
            document.getElementById('login-text').classList.remove('hidden');
            document.getElementById('login-loader').classList.add('hidden');
        }

        // --- SINGLE PAGE ROUTING ---
        document.querySelectorAll('.nav-item').forEach(link => {
            link.addEventListener('click', (e) => {
                e.preventDefault();
                if(!link.hasAttribute('data-route')) return;

                const route = link.getAttribute('data-route');
                
                document.querySelectorAll('.nav-item').forEach(el => {
                    el.classList.remove('active', 'bg-brand-50', 'text-brand-600');
                    el.classList.add('text-slate-500');
                });
                link.classList.remove('text-slate-500');
                link.classList.add('active', 'bg-brand-50', 'text-brand-600');
                
                document.querySelectorAll('.page').forEach(page => page.classList.remove('active'));
                document.getElementById(`page-${route}`).classList.add('active');
                
                document.getElementById('breadcrumbs-title').innerText = link.innerText.trim();
                
                if(window.innerWidth < 1024) toggleSidebar();
            });
        });

        function toggleSidebar() {
            document.getElementById('sidebar').classList.toggle('-translate-x-full');
            document.getElementById('mobileOverlay').classList.toggle('hidden');
        }

        // --- DATABASE & SINKRONISASI ---
        async function loadData() {
            document.getElementById('table-loader').classList.remove('hidden');
            try {
                if (APPS_SCRIPT_URL) {
                    const res = await fetch(`${APPS_SCRIPT_URL}?action=getProperties`);
                    const json = await res.json();
                    state.properties = json.data;
                } else {
                    // SINKRONISASI DATA UTAMA DARI HOMEPAGE
                    const localData = localStorage.getItem('kostspace_data');
                    if (localData) {
                        state.properties = JSON.parse(localData);
                    } else {
                        // Inisialisasi properti yang sama persis dengan yang ada pada Homepage
                        state.properties = [
                            {
                                id: 'PROP-1',
                                name: "Kost Premium Senopati Garden",
                                location: "Jakarta Selatan",
                                price: 3500000,
                                type: "Campur",
                                availability: "Tersedia",
                                image: "https://images.unsplash.com/photo-1522708323590-d24dbb6b0267?auto=format&fit=crop&q=80&w=800",
                                facilities: "WiFi Cepat, AC, Kamar Mandi Dalam, Parkir Mobil, Dapur Bersama",
                                address: "Kost Premium modern di area eksklusif Senopati, Jakarta Selatan."
                            },
                            {
                                id: 'PROP-2',
                                name: "Kuningan Smart Suites",
                                location: "Jakarta Selatan",
                                price: 2100000,
                                type: "Putri",
                                availability: "Tersedia",
                                image: "https://images.unsplash.com/photo-1598928506311-c55ded91a20c?auto=format&fit=crop&q=80&w=800",
                                facilities: "WiFi Cepat, AC, Kamar Mandi Dalam, Dekat MRT",
                                address: "Kost putri nyaman berjarak hanya 5 menit jalan kaki dari Rasuna Said."
                            },
                            {
                                id: 'PROP-3',
                                name: "Menteng Heritage Coliving",
                                location: "Jakarta Pusat",
                                price: 4500000,
                                type: "Campur",
                                availability: "Tersedia",
                                image: "https://images.unsplash.com/photo-1505691938895-1758d7feb511?auto=format&fit=crop&q=80&w=800",
                                facilities: "WiFi Cepat, AC, Kamar Mandi Dalam, Parkir Mobil, Kolam Renang",
                                address: "Rasakan kemewahan menginap di jantung kota Jakarta Pusat."
                            },
                            {
                                id: 'PROP-4',
                                name: "Tomang Cozy Stay Putra",
                                location: "Jakarta Barat",
                                price: 1800000,
                                type: "Putra",
                                availability: "Tersedia",
                                image: "https://images.unsplash.com/photo-1616486338812-3dadae4b4ace?auto=format&fit=crop&q=80&w=800",
                                facilities: "WiFi Cepat, AC, Dekat MRT",
                                address: "Sempurna bagi mahasiswa Universitas Trisakti maupun Tarumanagara."
                            }
                        ];
                        saveLocal();
                    }
                }
                renderDashboardStats();
                renderTable();
            } catch (error) {
                showToast('Gagal memuat data.', 'error');
            } finally {
                document.getElementById('table-loader').classList.add('hidden');
            }
        }

        function saveLocal() {
            localStorage.setItem('kostspace_data', JSON.stringify(state.properties));
        }

        // --- RENDER ESTIMASI STATISTIK ---
        function renderDashboardStats() {
            const total = state.properties.length;
            const active = state.properties.filter(p => p.availability !== 'Penuh').length;
            const rev = state.properties.reduce((sum, p) => sum + Number(p.price || 0), 0);
            
            const formatRev = new Intl.NumberFormat('id-ID', { style: 'currency', currency: 'IDR', maximumFractionDigits: 0 }).format(rev);
            
            document.getElementById('stats-container').innerHTML = `
                <div class="bg-white p-5 rounded-2xl border border-slate-150 shadow-soft">
                    <h3 class="text-[10px] font-extrabold uppercase tracking-widest text-slate-400 mb-2">Total Properti</h3>
                    <div class="text-2xl font-black text-slate-950">${total} Kost</div>
                </div>
                <div class="bg-white p-5 rounded-2xl border border-slate-150 shadow-soft">
                    <h3 class="text-[10px] font-extrabold uppercase tracking-widest text-slate-400 mb-2">Listing Aktif</h3>
                    <div class="text-2xl font-black text-slate-950">${active} Unit</div>
                </div>
                <div class="bg-white p-5 rounded-2xl border border-slate-150 shadow-soft">
                    <h3 class="text-[10px] font-extrabold uppercase tracking-widest text-slate-400 mb-2">Estimasi Omset</h3>
                    <div class="text-2xl font-black text-emerald-600">${formatRev}</div>
                </div>
                <div class="bg-white p-5 rounded-2xl border border-slate-150 shadow-soft">
                    <h3 class="text-[10px] font-extrabold uppercase tracking-widest text-slate-400 mb-2">Rasio Okupansi</h3>
                    <div class="text-2xl font-black text-brand-600">${total > 0 ? Math.round((active/total)*100) : 0}%</div>
                </div>
            `;
        }

        function renderTable(filterText = '') {
            const tbody = document.getElementById('properties-tbody');
            const empty = document.getElementById('empty-state');
            tbody.innerHTML = '';
            
            const filtered = state.properties.filter(p => 
                p.name.toLowerCase().includes(filterText.toLowerCase()) || 
                p.location.toLowerCase().includes(filterText.toLowerCase())
            );

            if (filtered.length === 0) {
                empty.classList.remove('hidden');
            } else {
                empty.classList.add('hidden');
                filtered.forEach(prop => {
                    const price = new Intl.NumberFormat('id-ID').format(prop.price);
                    
                    let availClass = 'bg-emerald-50 text-emerald-700 border-emerald-100';
                    if (prop.availability === 'Penuh') {
                        availClass = 'bg-rose-50 text-rose-700 border-rose-100';
                    }

                    let genderClass = 'bg-slate-100 text-slate-800';
                    if (prop.type === 'Putra') genderClass = 'bg-blue-50 text-blue-700 border-blue-100';
                    if (prop.type === 'Putri') genderClass = 'bg-rose-50 text-rose-700 border-rose-100';

                    const imgUrl = prop.image && prop.image !== '' ? prop.image : 'https://ui-avatars.com/api/?name=Kost&background=e2e8f0';

                    const tr = document.createElement('tr');
                    tr.className = 'hover:bg-slate-50/50 transition-colors group';
                    tr.innerHTML = `
                        <td class="px-6 py-4">
                            <div class="flex items-center gap-3">
                                <img src="${imgUrl}" class="w-10 h-10 rounded-xl object-cover border border-slate-200 shadow-sm shrink-0">
                                <div>
                                    <div class="font-bold text-slate-900">${prop.name}</div>
                                </div>
                            </div>
                        </td>
                        <td class="px-6 py-4">
                            <div class="font-bold text-slate-700">${prop.location}</div>
                            <span class="inline-block border text-[9px] font-extrabold px-2 py-0.5 rounded-md mt-1 uppercase ${genderClass}">${prop.type}</span>
                        </td>
                        <td class="px-6 py-4 font-black text-slate-950">Rp ${price}</td>
                        <td class="px-6 py-4">
                            <span class="inline-block border text-[10px] font-extrabold uppercase px-2.5 py-1 rounded-lg ${availClass}">
                                ${prop.availability}
                            </span>
                        </td>
                        <td class="px-6 py-4 text-right">
                            <button onclick="editProperty('${prop.id}')" class="text-slate-400 hover:text-brand-600 p-2 rounded-xl hover:bg-brand-50 transition-all">
                                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.5"><path d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"/></svg>
                            </button>
                            <button onclick="openDeleteModal('${prop.id}')" class="text-slate-400 hover:text-rose-600 p-2 rounded-xl hover:bg-rose-50 transition-all ml-1">
                                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.5"><path stroke-linecap="round" stroke-linejoin="round" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"/></svg>
                            </button>
                        </td>
                    `;
                    tbody.appendChild(tr);
                });
            }
        }

        document.getElementById('searchInput').addEventListener('input', (e) => {
            renderTable(e.target.value);
        });

        // --- CRUD ACTION MODALS ---
        const modal = document.getElementById('propertyModal');
        const modalContent = document.getElementById('propertyModalContent');
        
        function openPropertyModal() {
            document.getElementById('propertyForm').reset();
            document.getElementById('propId').value = '';
            document.getElementById('modalTitle').innerText = 'Tambah Properti Baru';
            document.getElementById('imagePreview').classList.add('hidden');
            state.base64Image = null;
            
            modal.classList.remove('hidden');
            setTimeout(() => {
                modal.classList.remove('opacity-0');
                modalContent.classList.remove('scale-95');
            }, 10);
        }

        function closePropertyModal() {
            modal.classList.add('opacity-0');
            modalContent.classList.add('scale-95');
            setTimeout(() => { modal.classList.add('hidden'); }, 300);
        }

        // Image Attachment Handler
        document.getElementById('propImage').addEventListener('change', function(e) {
            const file = e.target.files[0];
            if(file) {
                const reader = new FileReader();
                reader.onload = function(event) {
                    state.base64Image = event.target.result;
                    const preview = document.getElementById('imagePreview');
                    preview.querySelector('img').src = state.base64Image;
                    preview.classList.remove('hidden');
                };
                reader.readAsDataURL(file);
            }
        });

        async function submitPropertyForm() {
            const name = document.getElementById('propName').value;
            const loc = document.getElementById('propLocation').value;
            const price = document.getElementById('propPrice').value;
            
            if(!name || !loc || !price) {
                showToast('Mohon lengkapi field wajib.', 'error');
                return;
            }

            const id = document.getElementById('propId').value;
            const payload = {
                id: id,
                name: name,
                location: loc,
                price: price,
                type: document.getElementById('propType').value,
                availability: document.getElementById('propAvailability').value,
                whatsapp: document.getElementById('propWa').value,
                facilities: document.getElementById('propFacilities').value,
                address: document.getElementById('propAddress').value,
                image: state.base64Image || (id ? state.properties.find(p=>p.id===id).image : '')
            };

            const btnText = document.getElementById('saveBtnText');
            const loader = document.getElementById('saveLoader');
            btnText.classList.add('hidden'); loader.classList.remove('hidden');

            try {
                if(APPS_SCRIPT_URL) {
                    const res = await fetch(APPS_SCRIPT_URL, {
                        method: 'POST',
                        body: JSON.stringify({ action: 'saveProperty', payload: payload })
                    });
                    await res.json();
                    await loadData();
                } else {
                    if(id) {
                        const idx = state.properties.findIndex(p => p.id === id);
                        state.properties[idx] = {...state.properties[idx], ...payload};
                    } else {
                        payload.id = 'PROP-' + Date.now();
                        state.properties.unshift(payload);
                    }
                    saveLocal();
                    renderTable();
                    renderDashboardStats();
                }
                closePropertyModal();
                showToast('Properti kost berhasil disimpan!');
            } catch(e) {
                showToast('Gagal menyimpan properti.', 'error');
            } finally {
                btnText.classList.remove('hidden'); loader.classList.add('hidden');
            }
        }

        function editProperty(id) {
            const prop = state.properties.find(p => p.id === id);
            if(!prop) return;
            
            openPropertyModal();
            document.getElementById('modalTitle').innerText = 'Perbarui Properti Kost';
            document.getElementById('propId').value = prop.id;
            document.getElementById('propName').value = prop.name;
            document.getElementById('propLocation').value = prop.location;
            document.getElementById('propPrice').value = prop.price;
            document.getElementById('propType').value = prop.type;
            document.getElementById('propAvailability').value = prop.availability;
            document.getElementById('propWa').value = prop.whatsapp || '';
            document.getElementById('propFacilities').value = prop.facilities || '';
            document.getElementById('propAddress').value = prop.address || '';
            
            if(prop.image && prop.image !== '') {
                const preview = document.getElementById('imagePreview');
                preview.querySelector('img').src = prop.image;
                preview.classList.remove('hidden');
            }
        }

        // --- DELETE SYSTEM ---
        const delModal = document.getElementById('deleteModal');
        const delModalContent = document.getElementById('deleteModalContent');
        
        function openDeleteModal(id) {
            state.deleteId = id;
            delModal.classList.remove('hidden');
            setTimeout(() => {
                delModal.classList.remove('opacity-0');
                delModalContent.classList.remove('scale-95');
            }, 10);
        }

        function closeDeleteModal() {
            delModal.classList.add('opacity-0');
            delModalContent.classList.add('scale-95');
            setTimeout(() => { delModal.classList.add('hidden'); state.deleteId = null; }, 300);
        }

        async function confirmDelete() {
            if(!state.deleteId) return;
            
            const btnText = document.getElementById('delBtnText');
            const loader = document.getElementById('delLoader');
            btnText.classList.add('hidden'); loader.classList.remove('hidden');

            try {
                if(APPS_SCRIPT_URL) {
                    await fetch(APPS_SCRIPT_URL, {
                        method: 'POST',
                        body: JSON.stringify({ action: 'deleteProperty', id: state.deleteId })
                    });
                    await loadData();
                } else {
                    state.properties = state.properties.filter(p => p.id !== state.deleteId);
                    saveLocal();
                    renderTable();
                    renderDashboardStats();
                }
                closeDeleteModal();
                showToast('Properti kost berhasil dihapus.');
            } catch(e) {
                showToast('Gagal menghapus properti.', 'error');
            } finally {
                btnText.classList.remove('hidden'); loader.classList.add('hidden');
            }
        }

        // --- TOAST NOTIFICATIONS ---
        function showToast(message, type = 'success') {
            const toast = document.getElementById('toast');
            document.getElementById('toast-msg').innerText = message;
            
            if(type === 'error') {
                toast.firstElementChild.classList.replace('bg-slate-900', 'bg-rose-600');
                document.getElementById('toast-icon').innerText = '✕';
            } else {
                toast.firstElementChild.classList.replace('bg-rose-600', 'bg-slate-900');
                document.getElementById('toast-icon').innerText = '✓';
            }
            
            toast.classList.remove('translate-y-24', 'opacity-0');
            setTimeout(() => {
                toast.classList.add('translate-y-24', 'opacity-0');
            }, 3000);
        }

    </script>
</body>
</html>
